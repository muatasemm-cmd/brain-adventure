# تحديث محتوى اللعبة أونلاين

محتوى تحدي الكبار المنشور موجود في:

`web/content/adult_content.json`

لتحديث الأسئلة دون إصدار تطبيق جديد:

1. عدّل السؤال أو الإجابات داخل الملف.
2. ارفع رقم `version` في أعلى الملف.
3. غيّر `message` إلى وصف قصير للتحديث.
4. ارفع الملف إلى فرع `main` في GitHub.

بعد اكتمال GitHub Pages ستنزّل اللعبة الملف تلقائيًا عند فتح لوحة الكبار أو
عند الضغط على زر المزامنة. تحتفظ اللعبة بنسخة محلية وتستخدمها إذا انقطع
الإنترنت.

لإعادة إنشاء الملف كاملًا من بيانات المشروع:

```powershell
dart run tool/export_adult_content.dart
```

مهم: إعادة التوليد تستبدل التعديلات اليدوية داخل ملف JSON، لذلك تستخدم فقط
عند الرغبة في مزامنة الملف كاملًا مع البيانات المدمجة في التطبيق.
